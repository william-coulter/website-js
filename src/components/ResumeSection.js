function ResumeSection({ heading, cards }) {
  return (
    <div>
      <div class="font-sans text4-xl">{heading}</div>
      <div class="flex flex-col"></div>
    </div>
  );
}

export default ResumeSection;
